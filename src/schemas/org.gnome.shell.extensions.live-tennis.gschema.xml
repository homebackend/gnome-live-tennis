<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.live-tennis"
    path="/org/gnome/shell/extensions/live-tennis/">

    <key name="enabled" type="b">
      <default>false</default>
      <summary>Enable floating windows</summary>
      <description>Whether to show floating score windows for selected matches.</description>
    </key>

    <key name="auto-hide-no-live-matches" type="b">
      <default>true</default>
      <summary>Enable auto hiding</summary>
      <description>Whether to hide Live Score Window when no matches are live.</description>
    </key>

    <key name="only-show-live-matches" type="b">
      <default>true</default>
      <summary>Only show live matches</summary>
      <description>Only show live matches in Live View Window.</description>
    </key>

    <key name="update-interval" type="i">
      <default>15</default>
      <summary>Data update interval</summary>
      <description>The interval in seconds at which to fetch new match data.</description>
    </key>

    <key name="num-windows" type="i">
      <default>1</default>
      <summary>Number of floating windows</summary>
      <description>The maximum number of floating windows to display at once.</description>
    </key>

    <key name="match-display-duration" type="i">
      <default>10</default>
      <summary>Match display duration</summary>
      <description>The duration in seconds to display a single match before cycling to the next one,
        if multiple are selected.</description>
    </key>

    <key name="selected-matches" type="as">
      <default>[]</default>
      <summary>Selected match IDs</summary>
      <description>An array of match IDs for which to display floating windows.</description>
    </key>

    <key name="auto-view-new-matches" type="as">
      <default>[]</default>
      <summary>Selected event IDs</summary>
      <description>An array of event IDs for new matches are automatically added to live view.</description>
    </key>

    <key name="auto-select-live-matches" type="b">
      <default>false</default>
      <summary>Auto select live matchs</summary>
      <description>Whether to automatically select matches currently in progress.</description>
    </key>

    <key name="auto-select-country-codes" type="as">
      <default>[]</default>
      <summary>Auto select countries</summary>
      <description>Auto select matches from these countries.</description>
    </key>

    <key name="auto-select-player-names" type="as">
      <default>[]</default>
      <summary>Auto select Player Names</summary>
      <description>A comma-separated list of player names. Each name must contain only alphabetic characters. The name could be first name or last name.</description>
    </key>

    <key name="keep-completed-duration" type="i">
      <default>30</default>
      <summary>Keep completed matches duration</summary>
      <description>The duration in minutes to keep matches visible after they have finished.</description>
    </key>

    <key type="b" name="enable-atp">
      <default>true</default>
      <summary>Enable ATP tour</summary>
      <description>Process events and matches from ATP tour.</description>
    </key>

    <key type="b" name="enable-wta">
      <default>true</default>
      <summary>Enable WTA tour</summary>
      <description>Process events and matches from WTA tour.</description>
    </key>

    <key type="b" name="enable-atp-challenger">
      <default>false</default>
      <summary>Enable ATP Challenger tour</summary>
      <description>Process events and matches from ATP Challenger tour.</description>
    </key>

    <key name="live-window-size-x" type="i">
      <default>450</default>
      <summary>Live view window width</summary>
      <description>Width in pixels of Live view window (between 200 and 600).</description>
      <range min="200" max="600" />
    </key>

    <key name="live-window-size-y" type="i">
      <default>400</default>
      <summary>Live view window height</summary>
      <description>Height in pixels of Live view window (between 200 and 600).</description>
      <range min="200" max="600" />
    </key>

  </schema>
</schemalist>